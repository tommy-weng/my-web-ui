# my-web-ui
for web-ui study

# How to run

## Local
node app.js

## Docker
# build and run
# docker compose up --build
# then open http://localhost:3000
```bash
docker pull node:22-alpine
```

```bash
docker compose up --build
```